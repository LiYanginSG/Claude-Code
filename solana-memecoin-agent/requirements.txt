# Core (required) -------------------------------------------------------------
requests>=2.31          # market data over public REST endpoints
PyYAML>=6.0             # config.yaml

# LLM brain (optional) --------------------------------------------------------
# Without this the agent runs its deterministic fallback strategy instead.
anthropic>=0.40

# Live execution (optional) ---------------------------------------------------
# ONLY needed if you flip to live trading. Paper mode does not import it.
# ccxt>=4.4
